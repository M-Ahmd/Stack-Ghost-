// Minimal frontend starter script
console.log('Stack-Ghost frontend loaded');

document.addEventListener('submit', (ev) => {
  ev.preventDefault();
  // This is a placeholder to wire forms to the API later.
  alert('Form submitted — wiring to API later');
});
